import React, { Children } from 'react'
import { MainWrapper } from "../elements/MainElements"
import { motion } from 'framer-motion'
import { Container } from '../components'

export const MainContainer = ({ children }) => {
    console.log(children)
    return (
        <MainWrapper>

        </MainWrapper>)
}